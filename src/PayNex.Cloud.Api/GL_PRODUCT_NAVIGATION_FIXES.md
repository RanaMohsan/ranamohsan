# G/L, Product Action and Navigation Fixes

Applied fixes:

1. G/L Entries moved to a separate `gl-entries.html` page. Finance page no longer mixes chart/report maintenance with posted G/L entries.
2. Retail Products, Product Variants and Released Products now open separate pages instead of the same portal page. Duplicate product action labels were removed.
3. Left side panel was simplified. Actions available on the Home/Workspace page were removed from the left panel, except Company Information, which remains available in the left panel as requested.
4. Home/Workspace actions were cleaned so each action has one clear business purpose.
